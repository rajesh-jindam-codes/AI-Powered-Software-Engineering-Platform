'use client';

import React from 'react';
import CodebaseChatPage from '@/app/chat/page';

export default function RepositoryChatPage({ params }: { params: { id: string } }) {
  return <CodebaseChatPage />;
}
